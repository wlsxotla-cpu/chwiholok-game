# 진행 상황 (STATUS)

> 이 문서는 "어디까지 했고, 다음은 뭔가"를 담습니다.
> 자율 루프가 매 바퀴가 끝날 때 이 문서를 갱신합니다. 최신 상태가 항상 맨 위에 오도록 유지하세요.

## 마지막 갱신

- 시각: 2026-09-04 (자율 루프 시작 직전, 사람이 직접 남긴 초기 상태)
- 바퀴 번호: 0 (아직 루프가 실제 작업을 반영하지 않은 시작점)

## 지금까지 한 것

`docs/DESIGN.md`에 적힌 범위가 전부 이미 구현되어 있다 (캐릭터 5+1, 무기 9종
+ 진화 + 융합, 비급 5종, 적 5종 + 보스/웨이브/최종보스, 하드모드, 메타 진행,
맵 경계, HUD, 모바일 터치 컨트롤, 사운드까지). 즉 "0부터 시작"이 아니라
"이미 완성도 있는 프로토타입 위에서 다듬고 확장하는" 단계다. 세부 구현은
`scripts/`, `scenes/`를 직접 읽어서 확인할 것 — 이 문서는 요약일 뿐 코드가
정답이다.

## 지금 하고 있는 것

(비어 있음 — 다음 바퀴가 채울 것)

## 다음에 할 것

방향 지시가 없다면, 아래 순서로 우선순위를 판단할 것:
1. `docs/feedback/INBOX.md`에 지시가 있으면 그것부터.
2. 없다면 기존 시스템을 실제로 플레이/시뮬레이션해서 눈에 띄는 어색함, 밸런스
   문제, 시각적 불일치를 하나 찾아 고친다. 새 대형 시스템을 새로 벌이지 않는다
   (이미 콘텐츠는 충분히 많다 — 다듬는 게 우선).

## 막힌 것 / 확인이 필요한 것

- 배포용 웹 링크(cloudflare 임시 터널)는 로컬 서버·터널 프로세스가 살아있는
  동안만 유효하고, 재시작하면 주소가 바뀐다. 루프가 웹 빌드를 export해도 이
  프로세스들 자체가 죽어있으면 아무도 접속할 수 없다 — 살아있는지 확인이
  필요하면 STATUS.md에 남기고 사람에게 알릴 것, 루프가 직접 서버/터널을
  관리하지 않는다.

---

## 이전 기록

(오래된 갱신 내역은 아래로 누적)
