# 취호록 — Claude Code 작업 메모

무협 액션 로그라이크 (Godot 4.7, 웹 배포). 설계/진행상황 문서는 `docs/DESIGN.md`,
`docs/STATUS.md`, 자율 개발 루프는 `loop/`를 참고 (자세한 구조는 `README.md`).
이 파일은 대화형 세션에서 반복적으로 필요한 배포 절차와, 반복해서 터진 버그
패턴을 체크리스트로 정리한 것이다.

## 브랜치 구조

- `master`: 소스 브랜치.
- `production`: `master`를 fast-forward만으로 반영 (배포용 스냅샷, 별도 커밋 만들지 않음).
- `gh-pages`: 실제 배포된 정적 웹 빌드. 저장소 루트 = 프로덕션, `/dev/` = 스테이징.

## 배포 절차 (매번 이 순서대로)

1. 헤드리스 리임포트: `godot --headless --editor --quit-after 30`
2. 헤드리스 부트 체크: `godot --headless --quit-after 60` (에러 없는지 확인 —
   "N resources still in use at exit"는 정상 종료 시 나오는 잡음이라 무시해도 됨)
3. `scripts/game_state.gd`의 `VERSION` 올리고, `scripts/changelog_data.gd` 맨 위에
   새 버전 항목 추가
4. `git commit` (master) → push → `production`에 `merge --ff-only` → push
5. `godot --headless --export-release "Web" build/web/index.html`
6. **매번 필수**: export가 `build/web/index.service.worker.js`를 템플릿으로
   초기화하므로, install 리스너에 `self.skipWaiting();` 추가, activate 리스너의
   프로미스 체인 끝에 `.then(() => self.clients.claim())` 추가하는 두 군데를
   다시 패치해야 한다 (안 하면 사용자가 새 버전을 받으려면 탭을 완전히 닫았다
   열어야 함).
7. 임시 `git worktree add`로 `gh-pages` 체크아웃 → 루트와 `/dev/` 양쪽에 새 빌드
   복사. `.git`, `.nojekyll`은 절대 지우지 말 것 —
   `find "$WT" -maxdepth 1 -type f ! -name '.nojekyll' ! -name '.git' -exec rm -f {} \;`
   로 파일만 정리하고 `rm -rf`는 쓰지 않는다. 복사 후 `diff -rq`로 빌드 소스와
   완전히 같은지 확인 → commit/push → worktree 제거.
8. 실제 브라우저로 `?v=<임의문자열>`을 붙여 접속해 새 버전 changelog 팝업이
   뜨는지 확인 (GitHub Pages CDN 전파에 10~20초 정도 걸릴 수 있으니 안 뜨면
   좀 더 기다렸다 재시도할 것).

## 출시 전 체크리스트

새 기능을 만들거나 버그를 고친 뒤, 배포하기 전에 아래 중 해당하는 항목을 훑어볼 것.
전부 실제로 배포 후 발견된 사고에서 나온 항목이다.

- **사거리/판정 일치**: 조준·타겟팅 로직을 건드렸다면, 실제 공격 사거리(무기
  스탯)와 타겟 탐색 거리가 일치하는지 헤드리스로 경계값(사거리 안/밖 각각)을
  확인했는가? → 일자검이 자기 사거리보다 짧은 탐색 거리 때문에 사거리 안의
  보스를 못 잡던 버그가 실제로 있었음.
- **일시정지 → 재개**: 캐릭터 상태(부활, 레벨업, 융합 등)로 `get_tree().paused`가
  켜졌다 꺼지는 흐름을 건드렸다면, 물리 값(velocity 등)이 멈춘 값으로 남아있다가
  천천히 재적용되지 않는지 확인했는가? → 부활 직후 이동속도가 안 붙는 것처럼
  느껴지던 버그가 실제로 있었음.
- **모바일 탭 영역**: 새 클릭형 UI를 추가했다면, 아이콘의 "시각 크기"와 별개로
  탭 가능 영역(`custom_minimum_size`)이 최소 44~48px 이상인가? → 무기 아이콘이
  작아서 탭이 잘 안 되던 버그가 실제로 있었음.
- **반복 스폰 이펙트 상한**: 지속형 장판/파티클처럼 조건 충족 시마다 반복
  스폰되는 이펙트를 추가했다면, 동시 개수 상한이 있는가? (레벨이 오르면 스폰
  빈도·개수도 같이 늘어난다는 것까지 감안) → 염화호신진 화염 장판이 겹겹이
  쌓여 화면을 뒤덮던 버그가 실제로 있었음.
- **저장/이어하기 대칭성**: `_autosave()`에 새 진행 상태 필드를 추가했다면,
  "이어하기" 복원 로직(`main.gd`의 `resume_data` 처리)에도 대칭적으로
  반영했는가? 필드 위에 살아있던 엔티티(보스 등)는 스칼라 값만 복원해서는
  안 되고 실제로 다시 생성해줘야 하는가? → 중간보스가 살아있을 때 저장했다가
  "이어하기"로 불러오면 보스가 그냥 사라지던 버그가 실제로 있었음 (진행
  타이머만 저장하고 보스 엔티티 자체는 저장 안 했었음).
- **웹 전용 네트워크 버그**: 새 `HTTPRequest`를 추가했다면 `accept_gzip = false`를
  설정했는가? (기본값 true는 브라우저에서 이중 압축 해제로 응답이 깨짐 —
  네이티브 헤드리스 테스트로는 절대 재현되지 않고, 실제 배포 후 브라우저에서
  직접 확인해야만 드러남) → 랭킹 목록이 브라우저에서 항상 "기록 없음"으로
  뜨던 버그가 실제로 있었음.
- **Firestore 규칙 마이그레이션**: 닉네임/랭킹 관련 Firestore 보안 규칙을
  바꾼다면, 그 스키마가 생기기 전부터 있던 기존 사용자(레거시 데이터)가 그
  규칙 때문에 영구히 막히지 않는지 확인했는가? → 비밀번호 필드가 아예 없던
  기존 닉네임이 첫 검증 시도부터 영구 잠길 뻔한 케이스를 배포 직전에 발견해서
  고쳤음.
- **HUD 레이아웃 회귀**: HUD에 요소를 추가/크기를 바꿨다면, 헤드리스
  스크린샷으로 겹침이나 과도한 여백이 생기지 않았는지 실제로 눈으로
  확인했는가?

## 디버그 스크립트 규칙

- 헤드리스 검증용 임시 스크립트(`scripts/_debug_*.gd`, `scenes/_Debug*.tscn`)는
  검증이 끝나면 반드시 삭제하고, 커밋 전 `git status --short`로 진짜 소스
  파일만 남았는지 확인할 것.
